<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:padding="12dp">

    <TextView
        android:id="@+id/home_repo_name"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="레포지터리 이름"
        android:textSize="22sp"
        android:textStyle="bold"
        android:ellipsize="end"
        android:maxLines="1"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <TextView
        android:id="@+id/home_repo_description"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="레포지터리 설명"
        android:textSize="16sp"
        android:ellipsize="end"
        android:maxLines="1"
        android:layout_marginTop="12dp"
        app:layout_constraintStart_toStartOf="@+id/home_repo_name"
        app:layout_constraintTop_toBottomOf="@+id/home_repo_name" />

    <TextView
        android:id="@+id/home_repo_language"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="언어"
        android:textSize="16sp"
        android:layout_marginTop="8dp"
        app:layout_constraintStart_toStartOf="@+id/home_repo_description"
        app:layout_constraintTop_toBottomOf="@+id/home_repo_description" />
</androidx.constraintlayout.widget.ConstraintLayout>