<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.be_sopt_seminar">

    <uses-permission android:name="android.permission.INTERNET" />
    <application
        android:usesCleartextTraffic="true"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.BE_SOPT_Seminar">
        <activity android:name=".presentation.home.HomeActivity"/>
        <activity android:name=".presentation.signUp.SignUpActivity" />
        <activity android:name=".presentation.home.userInfo.UserInfoActivity">
        </activity>
        <activity android:name=".presentation.signIn.SignInActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>